version: "3.8"

services:
  web:
    image: nginx:alpine
    ports:
      - 8080:80
    volumes:
      - ./site.conf:/etc/nginx/conf.d/default.conf
      - ./app:/app
  php:
    image: php:8-fpm
    volumes:
      - ./app:/app
